import React from "react"

const myStyle = {
  color: "black",
}

const Resume = () => (
  <div
    render={() => (
      <>
        <main style={myStyle}>Hello World!</main>
      </>
    )}
  />
)

export default Resume
≠