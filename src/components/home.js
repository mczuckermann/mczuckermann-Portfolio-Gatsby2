import React from "react"

const Home = props => (
  <div>
    <div ref={props.refhome} className="container-full homeBody" />
    <div className="home-title">Matt Zuckermann</div>
  </div>
)

export default Home
